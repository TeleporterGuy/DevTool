/**
 * The dropdown look shared by every small popup menu — the sidebar's "+", the
 * tab bar's context menu, the composer's project menu. Positioning stays with
 * the caller; only the card and its rows are standardised here.
 */
export const menuCls = 'bg-surface border-[0.5px] border-border rounded-lg p-1 shadow-pop'

export const menuItemCls =
  'block w-full rounded-md px-2.5 py-1 bg-transparent border-0 text-text text-sm text-left cursor-pointer hover:bg-sel'
